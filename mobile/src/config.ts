// Update API_URL to point to your backend. For Android emulator use 10.0.2.2.
// For iOS simulator or when using Expo tunneling, use http://localhost:4000 or your ngrok url.
export const API_URL = "http://localhost:4000"; // <<-- change to your backend URL